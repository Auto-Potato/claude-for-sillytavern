# 发布约定

用户要求：每次上传或推送更新时，同时更新版本号和 CHANGELOG.md。

- 使用三段版本号 `主版本.次版本.补丁版本`，例如 `0.2.1`。
- 修复、小幅样式或交互调整：递增补丁版本，例如 `0.2.0 → 0.2.1`。
- 新增功能或一批功能性更新：递增次版本，补丁归零，例如 `0.2.1 → 0.3.0`。
- 重大改版或不兼容变更：评估递增主版本，其他位归零，例如 `0.3.0 → 1.0.0`。
- 根据相对于上次发布的整体改动判断级别，不按提交数量升级。
- 同步 manifest.json、package.json，以及存在时的包锁文件版本。
- 正式上传不保留 `-dev` 后缀；在最终回复中说明版本号。
- 不把本地预览草稿、个人预设或其他无关未跟踪文件加入发布。
